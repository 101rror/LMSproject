export function Table({ headers, rows }: { headers: string[]; rows: React.ReactNode[][] }) { return <div className="overflow-x-auto rounded-2xl border border-[var(--line)] bg-white"><table className="w-full text-left text-sm"><thead className="border-b border-[var(--line)] bg-[#f1f3ee] text-xs uppercase tracking-wider text-[#71807a]"><tr>{headers.map(header => <th key={header} className="px-5 py-4 font-bold">{header}</th>)}</tr></thead><tbody className="divide-y divide-[var(--line)]">{rows.map((row, i) => <tr key={i}>{row.map((cell, j) => <td key={j} className="px-5 py-4">{cell}</td>)}</tr>)}</tbody></table></div>; }
