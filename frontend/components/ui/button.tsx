import Link from 'next/link';
import { cn } from '@/lib/utils/cn';
export function Button({ children, href, variant = 'primary', className = '', type = 'button', onClick }: { children: React.ReactNode; href?: string; variant?: 'primary' | 'secondary' | 'ghost'; className?: string; type?: 'button' | 'submit'; onClick?: () => void }) { const styles = cn('inline-flex items-center justify-center rounded-full px-5 py-3 text-sm font-semibold transition hover:-translate-y-0.5', variant === 'primary' && 'bg-[var(--coral)] text-white hover:bg-[#d96b52]', variant === 'secondary' && 'border border-[var(--line)] bg-white text-[var(--ink)] hover:border-[var(--ink)]', variant === 'ghost' && 'text-[var(--ink)] hover:bg-white', className); return href ? <Link href={href} className={styles}>{children}</Link> : <button type={type} onClick={onClick} className={styles}>{children}</button>; }
