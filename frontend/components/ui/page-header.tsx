export function PageHeader({ eyebrow, title, description }: { eyebrow?: string; title: string; description?: string }) { return <header className="max-w-2xl space-y-3"><p className="text-xs font-bold uppercase tracking-[.18em] text-[var(--coral)]">{eyebrow}</p><h1 className="font-[family-name:var(--font-display)] text-4xl leading-tight md:text-5xl">{title}</h1>{description && <p className="text-lg leading-8 text-[#65736d]">{description}</p>}</header>; }
