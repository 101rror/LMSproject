import Link from 'next/link';
import { BookOpen, LayoutDashboard, LogOut, Menu, Trophy, UserRound } from 'lucide-react';
export function DashboardShell({ children }: { children: React.ReactNode }) { return <div className="min-h-screen bg-[#f1f3ee]"><aside className="fixed inset-y-0 left-0 hidden w-64 border-r border-[var(--line)] bg-[var(--paper)] p-7 lg:block"><Link href="/" className="font-[family-name:var(--font-display)] text-2xl">luma<span className="text-[var(--coral)]">.</span></Link><div className="mt-16 space-y-2"><Nav href="/dashboard" icon={<LayoutDashboard size={18} />} label="Overview" /><Nav href="/my-courses" icon={<BookOpen size={18} />} label="My courses" /><Nav href="/results" icon={<Trophy size={18} />} label="Results" /></div><div className="absolute bottom-8 left-7 right-7 border-t border-[var(--line)] pt-5"><Nav href="/" icon={<LogOut size={18} />} label="Sign out" /></div></aside><div className="lg:pl-64"><header className="flex items-center justify-between border-b border-[var(--line)] bg-[var(--paper)] px-6 py-5 lg:px-10"><button className="lg:hidden"><Menu size={22} /></button><span className="text-sm font-semibold text-[#65736d]">Thursday, August 27</span><div className="flex items-center gap-3 text-sm font-bold"><span className="grid h-9 w-9 place-items-center rounded-full bg-[var(--mint)]"><UserRound size={17} /></span><span className="hidden sm:block">Alex Morgan</span></div></header><main className="mx-auto max-w-7xl p-6 lg:p-10">{children}</main></div></div>; }
function Nav({ href, icon, label }: { href: string; icon: React.ReactNode; label: string }) { return <Link href={href} className="flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold text-[#65736d] hover:bg-white hover:text-[var(--ink)]">{icon}{label}</Link>; }
