import Link from 'next/link';
import { ArrowUpRight, Clock3, Layers3 } from 'lucide-react';
import type { Course } from '@/types/lms';
import { Badge } from '@/components/ui/badge';
export function CourseCard({ course, enrolled = false }: { course: Course; enrolled?: boolean }) { return <Link href={`/courses/${course.id}`} className="group block"><article className="overflow-hidden rounded-2xl border border-[var(--line)] bg-white transition hover:-translate-y-1 hover:shadow-xl hover:shadow-[#173b3f0d]"><div className={`relative flex h-44 items-end p-5 ${course.accent}`}><span className="absolute right-5 top-5 rounded-full bg-white/70 px-3 py-1 text-xs font-bold">{course.level}</span><div className="max-w-[220px] font-[family-name:var(--font-display)] text-3xl leading-8">{course.title}</div><ArrowUpRight className="absolute bottom-5 right-5 transition group-hover:translate-x-1 group-hover:-translate-y-1" size={22} /></div><div className="space-y-4 p-5"><Badge>{course.category}</Badge><p className="line-clamp-2 text-sm leading-6 text-[#65736d]">{course.description}</p><div className="flex items-center gap-4 border-t border-[var(--line)] pt-4 text-xs font-semibold text-[#7b8983]"><span className="flex items-center gap-1"><Layers3 size={14} /> {course.lessons} lessons</span><span className="flex items-center gap-1"><Clock3 size={14} /> {course.duration}</span></div>{enrolled && <div className="text-xs font-bold text-[var(--coral)]">Continue learning</div>}</div></article></Link>; }
