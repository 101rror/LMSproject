import { ArrowLeft, Check, Clock3, PlayCircle } from 'lucide-react';
import Link from 'next/link';
import { SiteHeader } from '@/components/layout/site-header';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import { courses } from '@/lib/api/data';
export default async function CourseDetails({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const course = courses.find(item => item.id === id) || courses[0]; return <><SiteHeader /><main className="mx-auto max-w-7xl px-6 py-12"><Link href="/courses" className="mb-12 inline-flex items-center gap-2 text-sm font-bold text-[#71807a]"><ArrowLeft size={16} /> Back to library</Link><div className="grid gap-12 lg:grid-cols-[1fr_380px]"><section><Badge tone="coral">{course.category}</Badge><h1 className="mt-5 max-w-3xl font-[family-name:var(--font-display)] text-6xl leading-none">{course.title}</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-[#65736d]">{course.description}</p><div className="mt-8 flex gap-6 text-sm font-semibold text-[#71807a]"><span className="flex items-center gap-2"><Clock3 size={17} /> {course.duration}</span><span className="flex items-center gap-2"><PlayCircle size={17} /> {course.lessons} lessons</span></div><div className="mt-16 border-t border-[var(--line)] pt-8"><h2 className="font-[family-name:var(--font-display)] text-3xl">Inside the course</h2><div className="mt-6 divide-y divide-[var(--line)]">{['Start with the signal', 'A map for messy ideas', 'From insight to direction', 'Make it memorable'].map((item, i) => <div key={item} className="flex items-center gap-4 py-5"><span className="text-sm font-bold text-[#9aa69f]">0{i + 1}</span><span className="font-semibold">{item}</span><Check className="ml-auto text-[#7daa97]" size={18} /></div>)}</div></div></section><aside className={`h-fit rounded-3xl ${course.accent} p-8 lg:sticky lg:top-8`}><div className="mb-28 text-sm font-bold">COURSE  /  {course.level.toUpperCase()}</div><p className="font-[family-name:var(--font-display)] text-3xl">Ready when you are.</p><p className="mt-3 text-sm leading-6 text-[#53665e]">Go at your own pace. Keep what works.</p><Button href="/register" className="mt-8 w-full">Enroll in course</Button></aside></div></main></> }
